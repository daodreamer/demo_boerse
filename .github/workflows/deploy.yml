name: Build & Deploy

on:
  push:
    branches: [with_docker]

jobs:
  build-php:
    name: Build PHP image
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build & push php
        uses: docker/build-push-action@v5
        with:
          context: .
          file: Dockerfile.php
          push: true
          tags: daodreamer/boerse-php:latest

  build-nginx:
    name: Build Nginx image
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build & push nginx
        uses: docker/build-push-action@v5
        with:
          context: .
          file: Dockerfile.nginx
          push: true
          tags: daodreamer/boerse-nginx:latest

  deploy:
    name: Trigger Azure redeploy
    runs-on: ubuntu-latest
    needs: [build-php, build-nginx]
    steps:
      - name: Notify Azure
        run: |
          STATUS=$(curl -s -o /tmp/azure_resp.txt -w "%{http_code}" -L -X POST "${{ secrets.AZURE_WEBHOOK_URL }}")
          echo "HTTP status: $STATUS"
          cat /tmp/azure_resp.txt
          [ "$STATUS" -ge 200 ] && [ "$STATUS" -lt 300 ] || exit 1
